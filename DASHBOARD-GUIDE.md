# دليل لوحة التحكم - Dashboard Guide

## نظرة عامة

تم إنشاء نظام لوحة تحكم آمن ومستقل يتيح إدارة جميع المحتويات الديناميكية في الموقع.

## المميزات

✅ **نظام مصادقة آمن**: تسجيل دخول منفصل عن WordPress Admin  
✅ **إدارة المستخدمين**: فقط مدير WordPress يمكنه إنشاء حسابات لوحة التحكم  
✅ **حماية كاملة**: لا يمكن الوصول للوحة التحكم بدون بيانات اعتماد صحيحة  
✅ **إدارة شاملة**: تعديل جميع المحتويات الديناميكية من مكان واحد  

---

## كيفية الاستخدام

### 1. إنشاء مستخدم لوحة التحكم (للمدير فقط)

1. سجل الدخول إلى **WordPress Admin** (`/wp-admin`)
2. اذهب إلى **المستخدمون** → **مستخدمي لوحة التحكم**
3. أدخل:
   - اسم المستخدم
   - البريد الإلكتروني
   - كلمة المرور
4. اضغط **إنشاء مستخدم**

**ملاحظة**: يمكنك إنشاء عدة مستخدمين للوحة التحكم.

---

### 2. تسجيل الدخول إلى لوحة التحكم

1. اذهب إلى: `http://localhost/sheikh-bassam-kayed/dashboard-login`
2. أدخل اسم المستخدم وكلمة المرور
3. اضغط **تسجيل الدخول**

---

### 3. استخدام لوحة التحكم

بعد تسجيل الدخول، ستصل إلى لوحة التحكم الرئيسية على:
`http://localhost/sheikh-bassam-kayed/dashboard`

#### الأقسام المتاحة:

**أ) روابط سريعة لإدارة المحتوى:**
- 📚 إدارة الكتب
- 🎤 إدارة المحاضرات الصوتية
- 📿 إدارة خطب الجمعة
- 🎥 إدارة الفيديوهات
- 🖼️ إدارة المعرض

**ب) تبويبات التعديل:**

1. **قسم البطل**:
   - تعديل نص المقدمة
   - رفع/تعديل صورة البطل (سطح المكتب)
   - رفع/تعديل صورة البطل (الموبايل)

2. **صفحة من نحن**:
   - رفع/تعديل صورة الشيخ
   - تعديل نبذة عن حياة الشيخ

3. **صفحة اتصل بنا**:
   - تعديل نص صفحة الاتصال

4. **التواصل الاجتماعي**:
   - إضافة/تعديل روابط:
     - فيسبوك
     - تويتر
     - يوتيوب
     - إنستغرام
     - تيليجرام
     - لينكد إن

5. **واتساب**:
   - تعديل رقم واتساب
   - تعديل الرسالة الافتراضية

---

## الأمان

### الميزات الأمنية:

1. **جلسات آمنة**: استخدام PHP Sessions للمصادقة
2. **Nonces**: حماية من CSRF attacks
3. **Sanitization**: تنظيف جميع المدخلات
4. **Role-based Access**: فقط مستخدمي `dashboard_user` يمكنهم الوصول
5. **Session Management**: الجلسات تنتهي عند تسجيل الخروج

### حماية الصفحات:

- `/dashboard-login`: متاح للجميع
- `/dashboard`: محمي - يتطلب تسجيل دخول

---

## إدارة المحتوى (CRUD)

### الكتب، المحاضرات، الفيديوهات، إلخ:

1. من لوحة التحكم، اضغط على الروابط السريعة (📚 إدارة الكتب، إلخ)
2. سيتم فتح WordPress Admin في تبويب جديد
3. يمكنك:
   - **إضافة**: اضغط "إضافة جديد"
   - **تعديل**: اضغط على العنصر المراد تعديله
   - **حذف**: اضغط "حذف" من قائمة العناصر

---

## استكشاف الأخطاء

### لا يمكن الوصول للوحة التحكم؟

1. تأكد من أن الصفحات تم إنشاؤها:
   - اذهب إلى WordPress Admin → الصفحات
   - تحقق من وجود "لوحة التحكم" و "تسجيل الدخول - لوحة التحكم"

2. إذا لم تكن موجودة:
   - اذهب إلى WordPress Admin مرة واحدة
   - سيتم إنشاؤها تلقائياً

### نسيت كلمة المرور؟

- فقط مدير WordPress يمكنه إعادة تعيين كلمة المرور:
  1. اذهب إلى WordPress Admin → المستخدمون
  2. ابحث عن المستخدم
  3. اضغط "تعديل"
  4. أدخل كلمة مرور جديدة

### لا تظهر التغييرات؟

- بعد التعديل، اضغط "حفظ التغييرات"
- تأكد من ظهور رسالة النجاح
- قم بتحديث الصفحة الأمامية

---

## ملاحظات مهمة

1. **الصور**: استخدم روابط مباشرة للصور (URLs) أو ارفعها إلى WordPress Media Library أولاً
2. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من إعدادات الموقع
3. **الأمان**: لا تشارك بيانات اعتماد لوحة التحكم مع أي شخص غير موثوق
4. **التحديثات**: جميع التغييرات تُحفظ فوراً في Customizer

---

## الدعم

إذا واجهت أي مشاكل:
1. تحقق من أن جميع الملفات موجودة في `wp-content/themes/sheikh-bassam-kayed/inc/`
2. تأكد من أن الصفحات موجودة في WordPress
3. تحقق من الأخطاء في WordPress Debug Log

